export type BaseInputProps = {
    error?: boolean;
    helperText?: string;
    id?: string;
    label?: string;
    // onChange?: JoyCheckboxProps['onChange'];
    size?: 'sm' | 'md' | 'lg';
    tooltip?: string;
};
